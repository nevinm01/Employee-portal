'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var Context = require('../Context.js');
var useFieldState = require('./useFieldState.js');

/* ----------------------- useScope ----------------------- */
function useScopedState() {
  var scope = React.useContext(Context.ScopeContext);
  return useFieldState.useFieldState(scope);
}

exports.useScopedState = useScopedState;
